# Intelligence layer — THE CORE USP
#
# verdict/   — deterministic lane (rules + Bayesian + behaviour → trust score)
#              NO model, NO LLM, fully explainable
# adaptive/  — rules-based skip/show logic
# assist/    — RAG + Gemma (suggest-only; never on accept/reject path)
