import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.satark.citizen',
  appName: 'SATARK Citizen',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
